/*
 *
 * Two crystal balls are dropped from the same height. Determine where
 * They will break. To do this, you are given an array of 0's and 1's.
 * It is a sorted array. When the 0's turn into 1's, return that index.
 *
 * Expects function:
 * Args: array<boolean>
 *
 * Best case scenario:
 * O(sqrt(n))
 *
 * Wosrst case scenario:
 * O(n)
 *
 */

export function crystal_ball_fast(arr: Array<number>): number {
  return 0;
}

export function crystal_ball(arr: Array<number>): number {
  return 0;
}
