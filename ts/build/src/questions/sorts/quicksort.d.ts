/**
 *
 * Implement quicksort:
 *
 * Takes args:
 * 1. data (Array<number>)
 * 2. target (number)
 *
 * finds the element in place
 *
 * returns index of element, or -1
 * if not in array.
 *
 */
export declare function quicksort(data: Array<number>, target: number): number;
